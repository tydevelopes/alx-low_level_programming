#include "main.h"

/**
 * main - checks last digit random number if it is > 5, 0 or < 6
 * Return: 0 if success
 */
int main(void)
{
	char *str = "_putchar";

	while (*str != '\0')
	{
		_putchar(*str);
		str++;
	}
	_putchar('\n');

	return (0);
}
